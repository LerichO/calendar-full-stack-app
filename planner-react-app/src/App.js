import React, { useState } from "react";
import { BrowserRouter as Router, Route, Routes, Link } from "react-router-dom";
import Planner from "./components/Planner";
import Redirect from "./components/redirect-btn";

function App() {

  return (
    <main>
      <Router>
        
      </Router>
    </main>
  );
}

export default App;
